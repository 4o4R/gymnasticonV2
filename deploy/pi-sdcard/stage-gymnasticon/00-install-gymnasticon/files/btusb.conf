# Workaround for some Broadcom/CSR USB Bluetooth adapters (e.g., Kinivo BTD-400).
# Forcing a controller reset on probe and disabling USB autosuspend helps avoid
# patch-load timeouts ("BCM: Patch command fc4c failed (-110)") seen on Pi Zero/Zero 2 W.
options btusb reset=1 enable_autosuspend=0
